<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- ContentProvider for early initialization of ActivityHolder -->
        <provider
            android:name="com.margelo.nitro.sharedtransition.SharedTransitionInitProvider"
            android:authorities="${applicationId}.sharedtransition.init"
            android:exported="false"
            android:initOrder="100" />
    </application>
</manifest>
